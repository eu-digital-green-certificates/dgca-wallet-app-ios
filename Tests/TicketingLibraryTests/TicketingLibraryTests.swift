import XCTest
@testable import TicketingLibrary

final class TicketingLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TicketingLibrary().text, "Hello, World!")
    }
}
